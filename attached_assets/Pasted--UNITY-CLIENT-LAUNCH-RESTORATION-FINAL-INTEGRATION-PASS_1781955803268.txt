# UNITY CLIENT LAUNCH RESTORATION — FINAL INTEGRATION PASS

## CONTEXT

Unity client is already fully built and deployed in project.

However web app is currently showing:
➡ "Unity Client (coming soon)" placeholder

This is incorrect.

We must restore full Unity launch pipeline.

DO NOT REBUILD UNITY.

ONLY FIX LAUNCH INTEGRATION.

---

## STEP 1 — REMOVE PLACEHOLDER UI

Search and DELETE:

* "Coming soon"
* "Unity Client placeholder component"
* Any hardcoded <ComingSoon /> or <BetaBanner />

Replace with real launcher.

---

## STEP 2 — CREATE UNITY LAUNCH CONFIG

Create:

/frontend/src/config/unityConfig.ts

```ts
export const UNITY_BUILD_URL =
  process.env.VITE_UNITY_URL || "/unity/index.html";

export const UNITY_ENABLED = true;
```

---

## STEP 3 — CREATE UNITY LAUNCH PAGE

Create:

/frontend/src/pages/UnityLauncher.tsx

Requirements:

* Full screen iframe
* Loads Unity build
* Auth token injected via postMessage
* Auto-resize canvas
* Loading spinner until Unity sends READY signal

Iframe:

```html
<iframe src={UNITY_BUILD_URL} />
```

---

## STEP 4 — AUTH BRIDGE (CRITICAL)

On login success:

Send token to Unity:

```ts
iframe.contentWindow.postMessage({
  type: "UNITY_AUTH",
  token: jwt,
  userId
}, "*");
```

Unity must already support this listener.

---

## STEP 5 — ROUTING FIX

Update routes:

* /login → login
* /dashboard → UnityLauncher (NOT placeholder)
* /world → UnityLauncher
* /kingdom → UnityLauncher

REMOVE all fallback "coming soon" screens.

---

## STEP 6 — UNITY BOOT HANDSHAKE

Frontend expects:

Unity sends:

```js
postMessage({
  type: "UNITY_READY"
})
```

Frontend must:

* hide loading screen
* unlock interaction

---

## STEP 7 — DEPLOYMENT COMPATIBILITY

Ensure Unity build works in:

* Render.com
* any static hosting
* iframe embedding

Path must be:

/public/unity/index.html

---

## FINAL RULE

If Unity build exists:

❌ NEVER show placeholder
❌ NEVER show coming soon

Always launch Unity.

---

## SUCCESS CRITERIA

After login:

✔ Unity loads inside iframe
✔ Camera is interactive
✔ World scene visible
✔ Kingdom selectable
✔ No placeholder screens anywhere

STOP AFTER IMPLEMENTATION.
