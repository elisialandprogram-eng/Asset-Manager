# UNITY LAUNCH FINAL HARDENING PASS (CRITICAL FIX)

## GOAL

Make Unity WebGL launch 100% reliable across:

* local dev
* Render.com
* production CDN
* iframe embedding

---

# STEP 1 — REMOVE ALL COMPETING ROUTES

Search entire frontend and DELETE:

* Any ComingSoon components
* Any fallback route for /dashboard /world /kingdom
* Any UNITY_ENABLED flags or conditional rendering
* Any “placeholder UI” logic

ONLY UnityLauncher must render those routes.

---

# STEP 2 — FORCE SINGLE ENTRY POINT

Update routing so ONLY THIS EXISTS:

```ts
{
  path: "/dashboard",
  element: <UnityLauncher />
},
{
  path: "/world",
  element: <UnityLauncher />
},
{
  path: "/kingdom",
  element: <UnityLauncher />
}
```

NO WRAPPERS, NO CONDITIONALS.

---

# STEP 3 — HARDEN UNITY LOAD DETECTION

Update UnityLauncher.tsx:

Replace postMessage logic with ROBUST handshake:

```ts
let unityReady = false;

window.addEventListener("message", (event) => {
  if (event.data?.type === "UNITY_READY") {
    unityReady = true;
    hideLoader();
    sendAuth();
  }
});
```

AND add retry fallback:

```ts
setTimeout(() => {
  if (!unityReady) {
    sendAuth(); // retry injection after 3s
  }
}, 3000);
```

---

# STEP 4 — FORCE UNITY BUILD BASE PATH

Inside Unity WebGL build:

EDIT index.html:

```js
var buildUrl = "Build";
```

ENSURE:

* Build folder is relative
* NOT absolute CDN paths
* NOT file:// references

---

# STEP 5 — FIX .WASM + MIME ISSUES (RENDER/NGINX)

Add static headers:

```
.wasm  application/wasm
.js    application/javascript
.data  application/octet-stream
```

If using Render:

Ensure:

* static publish directory = /public/unity
* NOT inside React build folder
* NOT bundled by Vite

---

# STEP 6 — PRELOAD UNITY BEFORE ROUTE LOAD

In App.tsx:

Add prefetch:

```ts
const unityPreload = new Image();
unityPreload.src = "/unity/Build/UnityLoader.js";
```

(ensures browser warms cache)

---

# STEP 7 — DEBUG MODE (MANDATORY)

Add debug overlay:

Show:

* Unity iframe status
* READY received (true/false)
* token sent (true/false)
* iframe loaded event

This prevents silent failures.

---

# FINAL SUCCESS CRITERIA

After login:

✔ Unity loads in <2 seconds
✔ No blank screen
✔ No infinite spinner
✔ World renders immediately
✔ Click + drag works
✔ Auth injected automatically
✔ No fallback UI exists anywhere

STOP AFTER COMPLETION.
