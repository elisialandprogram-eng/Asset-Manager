Continue development of **Eternal Kingdoms**.

We are now in **PHASE 1.2 — Live UI Integration Layer**.

---

IMPORTANT CONTEXT

Backend systems are already complete and MUST NOT be modified:

* Resource tick engine (server-side, 60s)
* Building upgrade system
* Upgrade queue system
* Kingdom state API
* Game engine in /game-engine
* PostgreSQL schema fully stable

DO NOT change backend logic, database schema, or game engine.

This task is frontend-only integration.

---

PRIMARY GOAL

Transform the existing Dashboard into a fully interactive LIVE KINGDOM CONTROL CENTER.

The game must feel like a real-time living kingdom.

---

FEATURES TO IMPLEMENT

1. LIVE RESOURCE HUD

Display real-time:

* Food
* Wood
* Stone
* Iron
* Gold

Also show:

* Production per minute for each resource
* Storage cap per resource
* Visual update every tick (or polling)

---

2. KINGDOM OVERVIEW PANEL

Show:

* Kingdom name
* Palace level
* Kingdom power (already calculated backend)
* Current tier
* Basic stats summary

---

3. BUILDING MANAGEMENT UI

For each building:

* Building name
* Level
* Production effect (read from API only)
* Upgrade button
* Upgrade cost preview (from backend /upgrade-preview endpoint)

DO NOT calculate anything in frontend.

---

4. UPGRADE SYSTEM UI

Implement:

* Upgrade modal (preview before confirm)
* Active upgrade queue display
* Countdown timer for ongoing upgrades
* Status labels:

  * idle
  * upgrading
  * completed

---

5. REAL-TIME UPDATE BEHAVIOR

Use either:

* Socket.IO OR
* Polling every 5–10 seconds

DO NOT over-engineer.

---

6. FRONTEND ARCHITECTURE RULES

* React components must be presentation only
* All game data must come from API
* No duplicated game logic in frontend
* No hardcoded values
* Use existing generated React Query hooks

---

STRICT PROHIBITIONS

DO NOT:

* Add world map
* Add combat system
* Add monsters
* Add alliances
* Modify backend logic
* Modify database schema
* Create new game mechanics
* Add Three.js or 3D rendering

ONLY enhance UI layer over existing systems.

---

UX GOAL

The player should feel:

"This is a living kingdom that is constantly evolving in real time."

---

OUTPUT REQUIREMENT

After completion:

* Provide updated UI components
* Show how each component connects to backend APIs
* Confirm NO backend changes were made
* Confirm real-time update mechanism used
* Confirm upgrade flow is fully functional from UI

STOP after completing Phase 1.2 UI layer.
