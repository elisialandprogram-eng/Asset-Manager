CRITICAL CONTEXT REQUIREMENT: Before writing or modifying any code, you MUST read the following three tracking documents completely to understand our project boundaries and architecture:
1. "ARCHITECTURE_STATE.md"
2. "PROJECT_MASTER.md"
3. "ROADMAP.md"

OBJECTIVE: Overhaul our frontend dashboard and 3D map visuals to transition away from text-based lists into a graphic-first, bright 2.5D isometric experience identical to League of Kingdoms. Fix any pre-existing rendering errors or runtime bugs causing the canvas element to show a blank/black screen.

Please execute the following tasks:

1. FIX PRE-EXISTING RENDERING & RUNTIME ERRORS
   - Audit the canvas setup on both the World Map and Dashboard. Ensure both World3DCanvas and KingdomScene3D are wrapped in explicit parent <div> containers with absolute dimensions (e.g., width: 100%; height: 100%; min-height: 550px;) to prevent zero-size collapse.
   - Resolve any silent console/WebGL compilation errors, missing refs, or boundary breaks that are preventing the Three.js canvas from rendering.

2. RENAME "WAR ROOM" TO "KINGDOM" & REMOVE TEXT CLUTTER
   - Rename all UI navigation headers, button triggers, and panel titles reading "War Room" to "Kingdom".
   - Completely remove the text-based list view toggle, text lists of buildings, and descriptive text structures within the central viewport. The viewport must be dedicated entirely to the 3D graphic view.

3. LEAGUE OF KINGDOMS VISUAL THEME & CAMERA LOCK
   - Change the canvas scene clear color from dark space black to a bright, vibrant medieval strategy sky blue ('#87CEEB').
   - In noiseUtils.ts and TerrainMesh.tsx, swap out muted olive tones for high-saturation, bright strategic grass-green biome colors.
   - In WorldCamera.tsx, lock the camera orientation into a non-rotatable 2.5D overhead strategy perspective. Hardcode the pitch down at a tilted fixed angle (~Math.PI / 3.5) and yaw at a 45-degree angle. Disable right-click and multi-touch scene rotation.

4. GRAPHIC INTERACTIVE BUILDING PLOTS
   - Restructure KingdomScene3D to arrange meshes using a clean, fixed-plot strategy layout:
     * Center Plot: A clear 3D Castle/Palace structure that visually scales with the current palace level tier.
     * Inner Ring: Specific interactive 3D plots for the Academy, Barracks, and Storage.
     * Outer Fields: A separate manufacturing group containing resource buildings (Farms, Lumber Camps, Mines).
   - Ensure clicking on any 3D building plot hooks seamlessly into our existing BuildModal or UpgradeModal state handlers.

POST-IMPLEMENTATION REQUIREMENT: After successfully verifying the visual and architectural modifications, update "ARCHITECTURE_STATE.md", "PROJECT_MASTER.md", and "ROADMAP.md" verbatim to reflect the new 2.5D graphic kingdom layout status and the completed fixes.